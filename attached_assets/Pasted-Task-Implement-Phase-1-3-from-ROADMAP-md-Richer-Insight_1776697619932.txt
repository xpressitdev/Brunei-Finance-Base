Task: Implement Phase 1.3 from ROADMAP.md — Richer Insights page
Read ROADMAP.md in the project root for full context. Then implement section 1.3 only.
Current state: The Insights page shows only 3 static observations (High Debt Load, Remaining Budget, Top Spending Category). The "Generate Insights" button's behavior is unclear. This needs to become the analytical heart of the app.
Requirements:
Add at least 6 insight types. Each should be a distinct card with a title, a primary value/finding, and a one-sentence plain-language explanation:

Spending trend vs. last month — total spend this month vs. last month, shown as a percentage change with an up/down arrow (e.g., "Spending up 12% vs. March").
Category anomaly detection — flag any category where this month's spend is ≥25% higher than the user's 3-month average for that category (e.g., "Dining up 40% vs. your usual").
Subscription creep — detect recurring transactions (same merchant, similar amount, monthly cadence) and show total monthly subscription load.
Savings rate — (income − total spend) / income, shown as a percentage with a benchmark (e.g., "You saved 11% this month. Healthy range: 20%+").
Debt-to-income trend — current DTI ratio and whether it's improving, stable, or worsening over the last 3 months.
Budget adherence — across all budgets set in the Cash Flow Plan, what % are on track vs. over (e.g., "3 of 5 budgets on track").

Visual requirements:

At least 2 of the 6 insights must include a visual element: a sparkline, a small month-over-month bar chart, or a simple progress indicator. Use the same chart library already used on the Dashboard for consistency.
Cards should have clear visual hierarchy: big number first, context second.

"Generate Insights" button:

On click, show a visible loading state (spinner or skeleton on the cards).
Re-run the AI analysis (call the existing DuitPlan AI service) and update any AI-generated commentary.
The 6 base insights above are computed from transaction data and do NOT require the AI — they should always be present, even without clicking the button. The AI button should add a 7th "AI Summary" card or enrich the commentary text on existing cards.
Disable the button while loading to prevent double-clicks.

Edge cases:

If a user has less than 1 month of data, show a "We need more data" empty state for trend-based insights (month-over-month, anomalies) but still show static ones (savings rate, DTI, budget adherence).
If a category has never been used, don't flag it as an anomaly.
Never show "up Infinity%" or "NaN" — sanitize all division-by-zero cases.

Out of scope — do NOT touch:

Dashboard page
Expense Tracker page
Any other page or feature outside Insights

Verification before stopping:

Navigate to Insights with the current user's data — all 6 cards render with real numbers.
Add a test transaction in a category that's 50% higher than usual — confirm anomaly card picks it up after refresh.
Click "Generate Insights" — loading state shows, AI card updates, button re-enables.
Check Insights with a fresh/empty user (if possible) — empty states render correctly, no NaN or crashes.

Stop after 1.3 is verified working. Do not proceed to 1.4 or any other task. Report back what you changed, which files were touched, and any edge cases you handled.